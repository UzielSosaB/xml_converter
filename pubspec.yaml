name: xml_converter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  excel: ^4.0.6
  file_picker: ^8.1.2
  flutter:
    sdk: flutter
  intl: ^0.19.0
  window_manager: ^0.4.2
  xml: ^6.5.0
  open_file: ^3.2.1
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
